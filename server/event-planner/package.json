{
  "name": "server",
  "version": "0.0.1",
  "description": "",
  "main": "index.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npm run start-dev",
    "format": "npx prettier --write src",
    "build": "npx prettier --check src && tsc",
    "start-dev": "npm run format && ts-node ./src/app.ts",
    "db-migrate": "cd sequelize && npx sequelize db:migrate --config ./config.json --migrations-path ./migrations",
    "postbuild": "cp package.json ./build/package.json && cd build && npm install --only=production --omit=dev"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "aws-serverless-express": "^3.4.0",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "knex": "^2.4.2",
    "pg": "^8.10.0",
    "pg-hstore": "^2.3.4",
    "reflect-metadata": "^0.1.13",
    "sequelize": "^6.29.3",
    "sequelize-typescript": "^2.1.5",
    "uuid": "^9.0.0"
  },
  "devDependencies": {
    "@types/aws-serverless-express": "^3.3.5",
    "@types/express": "^4.17.17",
    "@types/node": "^18.15.3",
    "@types/uuid": "^9.0.1",
    "@types/validator": "^13.7.14",
    "prettier": "^2.8.4",
    "sequelize-cli": "^6.6.0",
    "ts-node": "^10.9.1",
    "typescript": "^5.0.2"
  }
}
