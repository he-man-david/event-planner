{
  "name": "server",
  "version": "0.0.1",
  "description": "",
  "main": "index.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node index.js",
    "format": "npx prettier --write src",
    "build": "npx prettier --check src && tsc",
    "start-dev": "npm run format && ts-node ./src/index.ts",
    "start-prod": "npm run build && node ./build/index.js",
    "db-migrate": "cd sequelize && npx sequelize db:migrate --config ./config.json --migrations-path ./migrations"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "pg": "^8.10.0",
    "sequelize": "^6.29.3"
  },
  "devDependencies": {
    "@types/express": "^4.17.17",
    "prettier": "^2.8.4",
    "sequelize-cli": "^6.6.0",
    "ts-node": "^10.9.1",
    "typescript": "^5.0.2"
  }
}
