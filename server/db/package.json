{
  "name": "db",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "format": "npx prettier --write .",
    "build": "npm run format && tsc",
    "test": "echo \"Error: no test specified\" && exit 1",
    "db-migrate": "npx sequelize db:migrate --config ./config.json --migrations-path ./build/migrations"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "pg": "^8.10.0",
    "prettier": "^2.8.6",
    "sequelize": "^6.29.3",
    "sequelize-cli": "^6.6.0",
    "typescript": "^5.0.2"
  }
}
